# 2026-01-21-15-25 UV migration log

## Table of Contents
- [Purpose](#purpose)
- [Context](#context)
- [Commands executed](#commands-executed)
- [Notes](#notes)

## Purpose
Record the operational steps used to migrate mnemosys-core from Poetry tooling
to uv.

## Context
- Repository: mnemosys-core
- Branch: feature/uv-migration
- Issue: https://github.com/wphillipmoore/mnemosys-core/issues/189

## Commands executed
```
# Inspect uv version availability
python3 -m pip index versions uv | head -n 1

# Install pinned uv version
python3 -m pip install uv==0.9.26

# Verify uv CLI options
uv --help
uv help lock
uv help sync
uv help export

# Validate uv can lock with existing metadata
uv lock --dry-run

# Generate uv.lock with the updated pyproject.toml
uv lock

# Export requirements files from uv.lock
uv export --frozen --format requirements.txt --output-file requirements.txt --no-hashes --no-emit-project
uv export --frozen --format requirements.txt --output-file requirements-dev.txt --no-hashes --group dev --no-emit-project

# Sync the local environment with the new lockfile
uv sync --frozen --group dev

# Remove the legacy lockfile
rm <legacy-lockfile>
```

## Notes
- uv 0.9.26 is the pinned toolchain version for this migration.
- Dependency groups now use the `dependency-groups` table in `pyproject.toml`.
- Requirements exports are derived from `uv.lock` and remain the audit inputs.
